[project]
name = "repo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "librosa>=1.0.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.3",
    "quarto>=0.1.0",
    "quarto-cli>1.6.0",
]

[tool.uv]
python-preference = "only-system"
